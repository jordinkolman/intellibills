[project]
name = "indibills"
version = "0.1.0"
description = "A financial management API, tailored for gig workers and contractors"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [fastapi, sqlmodel, pydantic]
